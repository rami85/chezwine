<%= form_for(@grape_type) do |f| %>
  <% if @grape_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grape_type.errors.count, "error") %> prohibited this grape_type from being saved:</h2>

      <ul>
      <% @grape_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Nombre' %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label 'Activo' %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit 'GUARDAR' %>
  </div>
<% end %>
