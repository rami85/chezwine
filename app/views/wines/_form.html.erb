<%= form_for(@wine) do |f| %>
  <% if @wine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wine.errors.count, "error") %> prohibited this wine from being saved:</h2>

      <ul>
      <% @wine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :type_id %><br>
    <%= f.select :type_id, 
      options_from_collection_for_select(Type.all, :id, :name, @wine.type_id) %>
  </div>
  <div class="field">
    <%= f.label :appellation_id %><br>
    <%= f.select :appellation_id, 
      options_from_collection_for_select(Appellation.all, :id, :name, @wine.appellation_id) %>
  </div>
  <div class="field">
    <%= f.label :bottle_price %><br>
    <%= f.text_field :bottle_price %>
  </div>
  <div class="field">
    <%= f.label :glass_price %><br>
    <%= f.text_field :glass_price %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_area :color %>
  </div>
  <div class="field">
    <%= f.label :nose %><br>
    <%= f.text_area :nose %>
  </div>
  <div class="field">
    <%= f.label :mouth %><br>
    <%= f.text_area :mouth %>
  </div>
  <div class="field">
    <%= f.label :logo %><br>
    <%= f.text_field :logo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
